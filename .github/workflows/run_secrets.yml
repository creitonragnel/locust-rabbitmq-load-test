name: Run Get Secret

on:
  workflow_call:
    inputs:
      ID: ${{secrets.ID}}

jobs:
  run_locust_test:
    name: Run Get
    runs-on: ubuntu-latest
    steps:
      -
        uses: actions/checkout@v3
      - 
        run: echo ${{inputs.ID}}
